node_modules
dist
backend/dist
.env
.env.*
!.env.example
!.env.demo
playwright-report
test-results
.playwright-browsers
dev-dist
.DS_Store
coverage
*.log
*.tsbuildinfo

# AI session / personal artifacts (not project source)
agent-tools/
terminals/

# Generated mermaid diagrams (source is in .md files)
Docs/mermaid-diagram*.svg

# Note: mcps/ is committed so other developers get the same MCP tool definitions.

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfplan.*
*.tfvars
!terraform/terraform.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.lock.hcl

# Lambda build artifacts
backend/lambda-dist/
terraform/*.zip
terraform/tfplan

# Generated data / temp scripts from dev sessions
sales-for-canvas.csv
response.json
scripts/seed-*.ts
scripts/table.csv
scripts/dev-setup.cjs

# Local env (already covered but explicit)
.env.local
.env.*.local
